# Bonded Agent Demo Day 演示脚本

> 目标：在有限时间内让评委理解 **问题 → 方案 → 技术深度** 三个层次

---

## 时间分配（建议 5-7 分钟）

| 环节 | 时长 | 重点 |
|------|------|------|
| 问题引入 | 30s | 一句话建立共鸣 |
| 方案展示 | 1min | 流程图 + 保证输出公式 |
| 现场演示 | 2-3min | 完整用户流程 |
| 技术亮点 | 1min | 合约设计 + 架构 |
| 总结 | 30s | 愿景 + 下一步 |

---

## 1. 问题引入（30 秒）

> "当你让 AI Agent 帮你做一笔链上交易，它告诉你'预计能换到 100 个币'。你签名了。交易执行后只到账 80 个。Agent 不承担任何责任——因为'预计'不是'保证'。"

**关键停顿**：让评委意识到这是真实痛点——AI Agent 的预测没有金融责任约束。

> "Bonded Agent 解决的就是这个问题：让 Agent 的承诺变成真金白银的担保。"

---

## 2. 方案展示（1 分钟）

在屏幕上展示这张对比图（提前做一页 slide 或打开 README 流程图）：

```
传统 Agent：                          Bonded Agent：
"估计能换 100 tUSDC"                  "保证到账 ≥ 90 tUSDC，否则赔你 20 tUSDC"
   ↓                                     ↓
  实际到账 80                            实际到账 80
   ↓                                     ↓
  用户默默吃亏                          合约自动从保证金中赔付差额
  Agent 零责任                          保证金锁在合约里，纯代码执行
```

然后快速过一遍三种结算路径的幻灯片：

| 结果 | 触发条件 | 用户得到什么 |
|------|---------|------------|
| ✅ 正常 | 实际 ≥ 保证 | 全部到账 |
| 🔶 差额 | 底线 ≤ 实际 < 保证 | 差额赔付 |
| 🔴 失败 | Swap 失败 | 全额退款 + 5 tUSDC 补偿 |

---

## 3. 现场演示（2-3 分钟）—— 核心环节

**准备工作（演示前做完）**：
- [ ] 两个终端已启动（后端 :8787，前端 :3000）
- [ ] 浏览器已打开 http://localhost:3000，钱包已连接 Monad Testnet
- [ ] 钱包里有足够的 MON（至少 2-3 MON）
- [ ] 打开 MonadScan 浏览器窗口（备用展示链上数据）

### 演示流程

**Step 1：获取报价（30s）**

> "用户输入想要 Swap 的 MON 数量，点击获取报价。"

- 输入 1 MON → 点击"获取报价"
- 指着报价卡片逐项解释：
  - 预期输出（AI 模拟的结果）
  - 服务费 0.30%（运营商只收这点）
  - **保证输出**（高亮那行——这是核心）
  - 风险敞口（没被担保的部分，透明度）
  - 保证金 20 tUSDC（运营方真金白银锁进去了）

> "注意：保证输出低于预期输出——这个缓冲让运营方不会因为正常滑点被罚。但如果市场剧烈波动导致实际远低于预期，用户就能获得赔付。"

**Step 2：创建担保计划（30s）**

- 点击"创建担保计划"
- 钱包弹出确认 → 签名
- 等待 PlanCreated 事件确认

> "Plan 创建完成后，20 tUSDC 的保证金已经锁在合约里了。运营方不能单方面撤回。"

**Step 3：执行（1min）—— 区分两种模式**

先说明两种执行模式：
> "执行有两种方式：一是用户直接付 gas 上链执行（executePlan）；二是用户免费做 EIP-712 离线签名，由运营方代付 gas 提交（executePlanWithSignature）。第二种对用户零成本。"

演示 executePlanWithSignature（推荐，更能展示技术亮点）：
- 点击执行 → MetaMask 弹出签名请求（不是交易确认！）
- > "注意：用户只需要签名，不需要付 gas。这是 EIP-712 离线签名授权。"
- 签名后运营方代提交交易
- 等待确认 → 展示结果

**Step 4：结果展示（30s）**

- 显示最终到账金额
- 如果实际 ≥ 保证：显示 ✅ + 保证金已退还
- 在 MonadScan 上打开交易详情，指给评委看 PlanExecuted 事件

**加分项：演示失败赔付场景（如果有条件）**

- 准备第二个钱包，先创建 Plan，然后用高滑点参数执行
- 展示 ShortfallPaid 事件：用户收到了部分赔付
- > "即使市场不如预期，用户的损失也有上限——差额由合约自动赔付。"

---

## 4. 技术亮点（1 分钟）

> "我们解决的不只是 UI 层的承诺，而是一整套链上担保体系。"

挑 3 个最硬核的点：

**1. Try/Catch 原子执行**
> "合约内部用 try/catch 包裹 swap 调用：成功就检测输出是否达标，失败就进入赔付逻辑。整个过程在一个交易内原子完成，不存在中间状态。"

**2. EIP-712 离线签名 + 运营方代执行**
> "用户免费签名，运营方付 gas 提交。签名绑定 planId + inputAmount + deadline，防重放、防篡改。"

**3. 保证金安全机制**
> "每次执行后合约验证 `balanceOf(this) >= totalLockedBonds`——即使有人传入恶意 calldata 也不能窃取其他用户的保证金。"

---

## 5. 总结（30 秒）

> "Bonded Agent 要做的是：让 AI Agent 的每一次链上承诺，都有真金白银的担保。不是靠信誉、不是靠人工仲裁——靠智能合约。"

> "我们目前基于 Monad Testnet，支持 MON→tUSDC 的单向 Swap。下一步会扩展到更多交易对和更复杂的 Agent 操作。"

---

## 6. 可能被问到的问题 & 建议回答

| 问题 | 回答要点 |
|------|---------|
| **为什么运营方愿意锁保证金？** | 运营方通过服务费赚钱。保证输出比预期输出低约 10%，这个缓冲覆盖了正常滑点。只有市场剧烈波动时才会触发赔付——这是合理的商业风险。 |
| **保证比率 90% 怎么定的？** | 这是可配置参数。不同运营方可以根据自己的风险偏好设定不同比率，用户也可以选择更保守（高比率）或更便宜（低比率）的运营方。 |
| **如果运营方跑路？** | 保证金在创建 Plan 时就锁在合约里了，运营方无法单方面撤回。Plan 执行前保证金一直在合约中。 |
| **和保险协议有什么区别？** | 保险需要保费、理赔流程、人为判断。我们是即时、自动、代码执行的——Swap 结果可测量，不需要任何人判断"该不该赔"。 |
| **为什么用 Monad？** | Monad 的高性能 EVM 兼容链非常适合这种高频、低成本的 Agent 交易场景。我们的合约完全用 Solidity 编写，可以部署到任何 EVM 链。 |
| **和其他 AI Agent 项目的关系？** | 我们是 Agent 生态的"信任层"。任何做链上交易的 Agent 都可以接入 BondedExecutor 来为自己的预测提供担保——不需要改变自己的业务逻辑。 |

---

## 演示前检查清单

- [ ] Go 后端启动成功（`:8787`）
- [ ] Next.js 前端启动成功（`:3000`）
- [ ] 钱包已连接 Monad Testnet（Chain 10143）
- [ ] 账户有足够 MON gas 费（至少 2-3 MON）
- [ ] 新合约地址的 tUSDC 已 approve
- [ ] 浏览器没有多余标签页（干净演示环境）
- [ ] MonadScan 已打开（备用展示链上事件）
- [ ] 终端窗口已隐藏或缩小（不要露出命令行）
- [ ] 手机静音、通知关闭
- [ ] 准备好回退方案（如果 RPC 挂了，放录屏）

## 紧急回退方案

如果现场网络/RPC 出问题：
1. **提前录制一段完整流程的视频**（1-2 分钟）
2. 如果 RPC 挂了，直接放视频，同时口头解释
3. 视频不足以让评委理解技术深度时，切到合约代码（BondedExecutor.sol）讲解核心逻辑
